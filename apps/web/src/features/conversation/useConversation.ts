/**
 * Mantido por compatibilidade de importação.
 *
 * A lógica de conversa foi elevada para o `ConversationStore` (estado global e
 * persistente, acima das Áreas). Use `useConversations` de lá.
 */
export { useConversations } from './ConversationStore';
