# Nota

O lint usa **flat config** (`eslint.config.js`). Os pacotes `@eslint/js` e `globals`
são resolvidos transitivamente por `typescript-eslint`. Se o seu ambiente reclamar,
adicione-os explicitamente com:

```bash
npm i -D @eslint/js globals --workspace apps/web
```
